import { ModeView } from 'types';

/*=-=-=-=-=-= COMPONENT =-=-=-=-=-=*/

/*=-=-=-=-=-= STYLES =-=-=-=-=-=*/
export type ContainerProps = {
  modeViewType: ModeView;
};

export type LabelTypeProps = {
  isLabelType: boolean;
  modeViewType: ModeView;
};
